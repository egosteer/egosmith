# Core dependencies for the dataset pipeline
# (prepare / infer / filter / build / validate) and batch inference.
#
# The single-video demo (demo.py) renders with OpenCV (opencv-python below), so it
# needs nothing extra. The optional rerun-based WebDataset viewer is a separate group
# (see the `rerun` extra in pyproject.toml).
#
# Keep modern NumPy 1.x. Legacy MANO pickles import chumpy; use the patched
# chumpy source below instead of downgrading NumPy.
numpy==1.26.4
# HaWoR/legacy detector utilities still import pkg_resources, which was removed
# from setuptools 81+.
setuptools<81
opencv-python
decord
scikit-image
smplx==0.1.28
yacs
# mmcv==1.3.9
timm
einops
xtcocotools
pandas
hydra-core
hydra-submitit-launcher
hydra-colorlog
pytorch-lightning==2.2.4
torchmetrics==1.4.0
pyrootutils
rich
webdataset
ultralytics
pulp
supervision
pycocotools
joblib
natsort
# git+https://github.com/facebookresearch/pytorch3d.git@stable
# torch and torch-scatter are installed by scripts/setup/setup_env.sh from CUDA-specific
# wheel indexes; keep them out of this generic requirements file.
evo
pytorch-minimize
mmengine==0.10.4
plyfile
easydict
loguru
dill
lapx
# Needed only for loading legacy MANO pickle assets. Install the patched local
# copy generated by scripts/setup/fetch_chumpy.sh from thirdparty/chumpy_upstream:
# PyPI chumpy 0.70 is source-only, fails under default build isolation, and
# imports NumPy aliases removed in modern NumPy.
./thirdparty/chumpy
dashscope

## analyze:
zarr
seaborn
matplotlib

## Any4D (dense-depth stage) — normal dependency resolution. The CUDA torch stack
## and the --no-deps packages (rerun-sdk / uniception / utils3d / the Any4D source
## install) are handled by scripts/setup/setup_env.sh — those need a custom index / the
## --no-deps flag and can't live in a plain requirements file.
huggingface_hub
numba
opencv-python-headless==4.10.0.84
orjson
pillow-heif
pyarrow
python-box
safetensors
tensorboard
trimesh
jaxtyping
scikit-learn
minio
moderngl
